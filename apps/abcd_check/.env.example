# The ticket builder (tickets.py) needs no keys and no network beyond the one-time dataset download.
# The two-arm check that follows (issue 07) adds MUBIT_ENDPOINT, MUBIT_API_KEY, CLOUDFLARE_ACCOUNT_ID and CLOUDFLARE_API_TOKEN here.
