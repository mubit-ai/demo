# decide() ships in the next mubit-sdk release. Until then point MUBIT_SDK at a checkout of
# sdk/python/mubit-sdk (branch feat/jev-external); make adds it with --with and it overrides the pin.
mubit-sdk==0.13.2
python-dotenv==1.2.3
